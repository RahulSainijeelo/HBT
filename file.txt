Ok now create a quick login screen. this screen will ask user name and then create a folder in the storage. this folder will contain all the app data, like habbits and all other data of the applications. for one user there should be one file, if there are more than one file in this android folder for this app exist, then show a swich profile button and show the other profile name and reload the app with that profile and data. in the home screen give the user option to improt the profile by allowing him to import a file. also allow user to download the file from the profile. 

2. There should be three distinct tabs in the application. first is home page to show the calender. each day will open the screen for that day. this screen will contain two sections, tasks, where all the tasks shown and a habbit sections for showing the current tracking habits.

3. the second tab will be the dedicated tab for the todoist. it will be having 3 sub tabs (top), today, upcoming  and browse. and a plus button to add tasks. this add model should be advanced, that will allow adding date(that will open a model and it will be having few options, today, tomorrow, next week and no date and after that there will be a calender and after that there will be a add time, that will open a overlay like model and show current time and user can change hours from 0 to 24 and minutes from 0 to 60 or can select from the dynamic time dial that will first allow to select both hours and minutes, blow that show a duration button, that will allow to choose duration. after that confirmation and cancel button. in the date model show repeat button to make this task repeat in coming days). in the add task model, after the date button, show priority button and allow to select from 4 priorities, after that show a reminders button, this will open a model to place multiple reminders for the task lifetime (like at the time of task and in between and at completion). and after all these button, there will be a button to add this task into a label (inbox or custom created label, these lables will be visible in the browse sub tab of the todoist. 
4. the 3th tab is the habit tracker tab. this tab will allow user to add habbits (with all advanced features those should be there for a advanced habit tracker app.)

5. In the header show app icon and show profile button (with profile picture). this will open the profile screen, this screen will allow downloading the data till current app data(also all app data should be automatically being saved in the said location user data file in the android folder, so that there will not be any data loss issue. 

6. don't forget that ui will be exactly like nothing os 4 ui



adb install android/app/build/outputs/apk/release/app-release.apk
./gradlew assembleRelease




//////////export ANDROID_HOME=$HOME/Android/Sdk && cd android && ./gradlew assembleDebug --parallel 2>&1 | tail -30



//////adb install -r android/app/build/outputs/apk/debug/app-debug.apk && echo "Installed successfully"

adb logcat -d | grep -i "error\|exception\|RiseWidget" | grep -v "nothing\|aiwidget" | tail -30